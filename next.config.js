/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : [
            "cdn.shopify.com",
            "images.pexels.com"
        ]
    }
}

module.exports = nextConfig
